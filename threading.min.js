var w;
function startWorker() {
  "undefined" != typeof Worker
    ? (void 0 === w && (w = new Worker("api.js")),
      (w.onmessage = function (e) {
        const n = e.data;
        addElement(n.title, n.description, n.urlToImage, n.source.name);
      }))
    : console.log("Sorry! No Web Worker support.");
}
function stopWorker() {
  w.terminate(), (w = void 0);
}
function addElement(e, n, r = "", d) {
  let i = `<div class="card">\n          <div class="card-image" style="background: lightgray url('${r}') no-repeat center;background-size: cover;">\n              <div class="card-tag">${d}</div>\n          </div>\n          <div class="card-content">\n              <div class="card-headline">${e}</div>\n              <div class="card-description">${n}</div>\n          </div>\n      </div>`;
  document.querySelector(".card-container").innerHTML += i;
}
